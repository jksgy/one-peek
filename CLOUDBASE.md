# 一眼即焚 OnePeek · 部署到腾讯云 CloudBase（云托管）

> 你使用的是 CloudBase 云开发平台（tbc.cloud.tencent.com），本应用通过「云托管」以 Docker 容器方式部署，代码无需任何修改。
> 本应用自包含（自带文件存储、焚毁状态管理），**无需开通** SQL 数据库、对象存储等其它功能。

---

## 部署步骤（约 5 分钟）

### 1. 开通云托管

控制台左侧 → **「云函数 / 托管」** → 选择 **云托管** 标签页（如首次使用按提示开通，计费选**按量付费**）。

### 2. 创建服务

- 服务名称：`onepeek`（随意）
- 地区：就近选择

### 3. 上传代码包并配置

选择 **本地上传代码包**，上传 `one-peek-cloudbase.zip`（包根目录已含 Dockerfile），关键配置：

| 配置项 | 建议值 | 说明 |
| --- | --- | --- |
| 监听端口 | **3456** | 程序监听的端口（也兼容 PORT 环境变量） |
| CPU / 内存 | 0.5 核 / 1GB | 视频 300MB 上传够用，不够再调 |
| 实例数最小 | **0 或 1**（见下方重要提醒） | 0 = 闲置缩容省钱；1 = 内容按承诺保留 |
| 实例数最大 | **1**（必须！） | 多实例会导致"已焚毁"状态在不同实例间不同步 |
| 请求超时 | 300 秒以上 | 大文件上传需要 |

点击发布，等待构建完成。

### 4. 访问

发布成功后，云托管会提供一个**默认域名**（形如 `https://xxx.ap-shanghai.run.tcloudbase.com`），在服务详情/「HTTP 访问服务」里能看到。浏览器打开就是上传页，把链接发给别人即可使用。

---

## ⚠️ CloudBase 特有注意事项（必读）

### 1. 缩容到 0 后磁盘会被清空

云托管实例闲置一段时间后会回收，回收时**尚未被查看的文件会丢失**（已焚毁的不受影响）。两种取舍：

- **方案 a（省钱）**：最小实例数 = 0，接受"闲置后未查看内容丢失"。阅后即焚通常几分钟内就看完了，建议把分享时效选短档（30 分钟 / 1 小时）。
- **方案 b（可靠）**：最小实例数 = 1，服务常驻，内容按承诺保留最长 7 天，但会产生持续费用（按量计费约几十元/月，具体看官网价格页）。

### 2. 上传大小与超时

云托管对请求体大小、请求时长有平台限制。如果大文件上传报错（413/超时）：

- 把 `server.js` 第 21 行 `MAX_SIZE` 调小（如 `50 * 1024 * 1024` = 50MB）
- 把云托管的请求超时时间调大

### 3. 计费

- 云托管按 CPU/内存/流量**按量计费**，你参与的活动赠送额度（大模型 Token、金豆额度等）**不含**云托管资源费用，请在费用中心留意。
- 实例最小 0 时，只有有人访问才计费，费用会非常低，个人使用通常每月几块钱以内。

---

## 常见问题

| 现象 | 解决 |
| --- | --- |
| 构建失败 | 确认上传的是 `one-peek-cloudbase.zip`，且 Dockerfile 在包根目录 |
| 打开域名 502/503 | 检查监听端口是否填了 3456；实例是否已扩容（最小 0 时首次访问要冷启动几秒） |
| 上传 300MB 失败 | 见上方"上传大小与超时"，调小 MAX_SIZE |
| 想绑自己的域名 | 云托管服务设置 → 自定义域名（需备案），或用「HTTP 访问服务」配置 |
| 改了代码要更新 | 重新上传代码包发布即可 |

## 安全提醒

- 上传页是公开的，任何人都能向你的云托管上传文件。若只想自己上传，可在云托管服务设置里配置**访问鉴权**（Basic Auth），查看链接不受影响。
- 内容查看一次后逻辑即焚毁；文件在被查看 1 小时后物理删除，未查看的最长保留 7 天自动焚毁。
- 建议在分享场景中尽量使用 HTTPS 链接（云托管默认域名本身就是 HTTPS，这点比自建服务器更方便）。
